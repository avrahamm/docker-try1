version: '3'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/var/www/html
    ports:
      - "8081:80"
    environment:
      XDEBUG_CONFIG: "remote_host=host.docker.internal remote_log = /tmp/xdebug_log"

